// import { WaitlistState } from '@/types';
// import { STORAGE_KEYS } from './constants';

// export function getStoredWaitlistState(): WaitlistState | null {
//   const saved = localStorage.getItem(STORAGE_KEYS.WAITLIST_STATE);
//   return saved ? JSON.parse(saved) : null;
// }

// export function saveWaitlistState(state: WaitlistState): void {
//   localStorage.setItem(STORAGE_KEYS.WAITLIST_STATE, JSON.stringify(state));
// }

// export function getStoredPartyId(): string | null {
//   return localStorage.getItem(STORAGE_KEYS.CURRENT_PARTY);
// }

// export function savePartyId(partyId: string): void {
//   localStorage.setItem(STORAGE_KEYS.CURRENT_PARTY, partyId);
// }